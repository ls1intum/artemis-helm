apiVersion: v1
kind: Service
metadata:
  name: activemq-broker
spec:
  ports:
    - name: stomp
      port: {{ .Values.broker.service.port }}
      protocol: TCP
      targetPort: activemq-stomp
        #    - name: 8161tcp
        #      port: 8161
        #      protocol: TCP
        #      targetPort: 8161
  selector:
    app: activemq-broker
